{
  "name": "mrnarchitect",
  "devDependencies": {
    "frontend": "workspace:*"
  },
  "scripts": {
    "check": "pnpm run --filter frontend check",
    "fix": "pnpm run --filter frontend fix",
    "build": "pnpm run --filter frontend build",
    "start": "uv run litestar --app mrnarchitect.app:app run --host 0.0.0.0 --port 8080"
  }
}
