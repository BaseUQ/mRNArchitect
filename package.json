{
  "name": "mrnarchitect",
  "devDependencies": {
    "@biomejs/biome": "^2.3.2",
    "@playwright/test": "^1.56.1",
    "frontend": "workspace:*",
    "typescript": "~5.9.3",
    "vitest": "^4.0.4"
  },
  "scripts": {
    "build": "pnpm run --filter frontend build",
    "start": "litestar --app mrnarchitect.app:app run --host 0.0.0.0 --port 8080",
    "check": "biome check",
    "fix": "biome check --fix"
  }
}
